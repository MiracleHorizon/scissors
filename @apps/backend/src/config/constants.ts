export const ENVIRONMENT = {
  DEVELOPMENT: 'development',
  PRODUCTION: 'production'
} as const

export const PRODUCTION_ENV_FILE = '.env.production'
export const DEVELOPMENT_ENV_FILE = '.env.development'

export const DEFAULT_PORT = 4200
